$('#posts').append("<%= escape_javascript(render 'images')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@images, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }